How to use:

1.open the command prompt and navigate to the folder containing the mySampleApp.exe; the base folder has mySampleApp\bin\debug\net8.0 as the directory

2.use: mySampleApp.exe -o "file.jpg" -t "I like cats" -d "Specify the directory where you want to download the cat pic" 